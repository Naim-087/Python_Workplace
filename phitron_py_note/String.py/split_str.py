my_str='split(separator): Splits a string on a ' \
'specified separator into a list of strings. ' \
'If no separator is specified, it splits on whitespace.'

split_word=my_str.split()
print(split_word)